# {{ msg("view.revision_log.title") }}

{{ msg("view.revision_log.intro") }}

{% for item in items -%}
## `{{ item.log_id }}` / {{ item.summary }}

- `log_order`: {{ item.log_order }}
- `status`: {{ item.status }}
- `operator_role`: {{ item.operator_role }}
- `plan_action_ids`: {{ item.plan_action_ids or '' }}
- `thread_ids`: {{ item.thread_ids or '' }}
- `created_at`: {{ item.created_at or '' }}
- {{ msg("view.revision_log.change_note") }}: {{ item.change_note or '' }}
- {{ msg("view.revision_log.response_note") }}: {{ item.response_note or '' }}

| `entry_order` | `target_file` | `target_locator` | `change_type` | {{ msg("view.revision_log.change_summary") }} | {{ msg("view.revision_log.rationale") }} | {{ msg("view.revision_log.evidence_source") }} | {{ msg("view.revision_log.expected_response_use") }} |
| --- | --- | --- | --- | --- | --- | --- | --- |
{% for entry in item.entries -%}
| {{ entry.entry_order }} | {{ entry.target_file or '' }} | {{ entry.target_locator or '' }} | {{ entry.change_type }} | {{ entry.change_summary }} | {{ entry.rationale or '' }} | {{ entry.evidence_source or '' }} | {{ entry.expected_response_use or '' }} |
{% else -%}
|  |  |  |  |  |  |  |  |
{% endfor %}

{% else -%}
## {{ msg("view.revision_log.no_logs") }}

{{ msg("view.revision_log.no_log_data") }}
{% endfor %}
