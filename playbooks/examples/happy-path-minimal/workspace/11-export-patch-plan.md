# Export Patch Plan

这是从 `review-master.db` 渲染出的 Stage 6 导出补丁计划视图。数据库真源是 `export_patch_sets` 与 `export_patches`。导出脚本只负责复制导出副本，并按这里对应的显式锚点执行插入或替换；它不负责语义判断，也不负责生成最终文案。

## happy_clean_patch_set

- 导出类型: clean_manuscript
- 来源根目录: /home/joshua/Workspace/Code/Skill/review_master/playbooks/examples/happy-path-minimal/inputs/manuscript
- 输出根目录: /home/joshua/Workspace/Code/Skill/review_master/playbooks/examples/happy-path-minimal/outputs/revised-manuscript
- 状态: ready

| 序号 | 意见 | 动作 | 位置 | 目标文件 | 操作 | 锚点文本 | Marked 文本 | Clean 文本 | 备注 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 1 | atomic_001 | 1 | 1 | main.tex | replace | \documentclass{article}\n\usepackage{booktabs}\n\n\title{Transformer-Based Crop Disease Diagnosis}\n\author{Example Author}\n\date{}\n\n\begin{document}\n\maketitle\n\n\section{Introduction}\nCrop disease diagnosis from leaf images remains difficult when visually similar diseases appear under different lighting conditions. We study whether a vision transformer can improve classification quality over a conventional CNN baseline on this task.\n\n\section{Method}\nOur model replaces the CNN feature extractor with a transformer encoder and keeps the same classifier head and training recipe as the baseline.\n\n\section{Results}\nTable~\ref{tab:main-results} reports the comparison between the CNN baseline and our transformer model.\n\n\begin{table}[h]\n\centering\n\caption{Classification accuracy on the held-out test set.}\n\label{tab:main-results}\n\begin{tabular}{lc}\n\toprule\nModel & Top-1 Accuracy \\\n\midrule\nCNN baseline & 89.4 \\\nTransformer & 92.1 \\\n\bottomrule\n\end{tabular}\n\end{table}\n\nThe transformer model improves top-1 accuracy by 2.7 points over the CNN baseline.\n\n\section{Conclusion}\nThe transformer model provides a simple accuracy improvement on this dataset.\n\n\end{document}\n | \documentclass{article}\n\usepackage{booktabs}\n\n\title{Transformer-Based Crop Disease Diagnosis}\n\author{Example Author}\n\date{}\n\n\begin{document}\n\maketitle\n\n\section{Introduction}\nCrop disease diagnosis from leaf images remains difficult when visually similar diseases appear under different lighting conditions. We study whether a vision transformer can improve classification quality over a conventional CNN baseline on this task.\n\n\section{Method}\nOur model replaces the CNN feature extractor with a transformer encoder and keeps the same classifier head and training recipe as the baseline.\n\n\section{Results}\nTable~\ref{tab:main-results} reports the comparison between the CNN baseline and our transformer model.\n\n\begin{table}[h]\n\centering\n\caption{Classification accuracy on the held-out test set.}\n\label{tab:main-results}\n\begin{tabular}{lc}\n\toprule\nModel & Top-1 Accuracy \\\n\midrule\nCNN baseline & 89.4 \\\nTransformer & 92.1 \\\n\bottomrule\n\end{tabular}\n\end{table}\n\n\replaced{The transformer improves performance because global-context modeling reduces confusion between visually similar disease patterns while retaining discriminative lesion structure. The supporting evidence for this mechanism-based explanation is provided in the paragraph immediately below Table~2.}{The transformer model improves top-1 accuracy by 2.7 points over the CNN baseline.}\n\n\section{Conclusion}\nThe transformer model provides a simple accuracy improvement on this dataset.\n\n\end{document}\n | \documentclass{article}\n\usepackage{booktabs}\n\n\title{Transformer-Based Crop Disease Diagnosis}\n\author{Example Author}\n\date{}\n\n\begin{document}\n\maketitle\n\n\section{Introduction}\nCrop disease diagnosis from leaf images remains difficult when visually similar diseases appear under different lighting conditions. We study whether a vision transformer can improve classification quality over a conventional CNN baseline on this task.\n\n\section{Method}\nOur model replaces the CNN feature extractor with a transformer encoder and keeps the same classifier head and training recipe as the baseline.\n\n\section{Results}\nTable~\ref{tab:main-results} reports the comparison between the CNN baseline and our transformer model.\n\n\begin{table}[h]\n\centering\n\caption{Classification accuracy on the held-out test set.}\n\label{tab:main-results}\n\begin{tabular}{lc}\n\toprule\nModel & Top-1 Accuracy \\\n\midrule\nCNN baseline & 89.4 \\\nTransformer & 92.1 \\\n\bottomrule\n\end{tabular}\n\end{table}\n\nThe transformer improves performance because global-context modeling reduces confusion between visually similar disease patterns while retaining discriminative lesion structure. The supporting evidence for this mechanism-based explanation is provided in the paragraph immediately below Table~2.\n\n\section{Conclusion}\nThe transformer model provides a simple accuracy improvement on this dataset.\n\n\end{document}\n | Replace the full happy-path manuscript with the confirmed marked and clean variants. |

## happy_marked_patch_set

- 导出类型: marked_manuscript
- 来源根目录: /home/joshua/Workspace/Code/Skill/review_master/playbooks/examples/happy-path-minimal/inputs/manuscript
- 输出根目录: /home/joshua/Workspace/Code/Skill/review_master/playbooks/examples/happy-path-minimal/outputs/marked-manuscript
- 状态: ready

| 序号 | 意见 | 动作 | 位置 | 目标文件 | 操作 | 锚点文本 | Marked 文本 | Clean 文本 | 备注 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 1 | atomic_001 | 1 | 1 | main.tex | replace | \documentclass{article}\n\usepackage{booktabs}\n\n\title{Transformer-Based Crop Disease Diagnosis}\n\author{Example Author}\n\date{}\n\n\begin{document}\n\maketitle\n\n\section{Introduction}\nCrop disease diagnosis from leaf images remains difficult when visually similar diseases appear under different lighting conditions. We study whether a vision transformer can improve classification quality over a conventional CNN baseline on this task.\n\n\section{Method}\nOur model replaces the CNN feature extractor with a transformer encoder and keeps the same classifier head and training recipe as the baseline.\n\n\section{Results}\nTable~\ref{tab:main-results} reports the comparison between the CNN baseline and our transformer model.\n\n\begin{table}[h]\n\centering\n\caption{Classification accuracy on the held-out test set.}\n\label{tab:main-results}\n\begin{tabular}{lc}\n\toprule\nModel & Top-1 Accuracy \\\n\midrule\nCNN baseline & 89.4 \\\nTransformer & 92.1 \\\n\bottomrule\n\end{tabular}\n\end{table}\n\nThe transformer model improves top-1 accuracy by 2.7 points over the CNN baseline.\n\n\section{Conclusion}\nThe transformer model provides a simple accuracy improvement on this dataset.\n\n\end{document}\n | \documentclass{article}\n\usepackage{booktabs}\n\n\title{Transformer-Based Crop Disease Diagnosis}\n\author{Example Author}\n\date{}\n\n\begin{document}\n\maketitle\n\n\section{Introduction}\nCrop disease diagnosis from leaf images remains difficult when visually similar diseases appear under different lighting conditions. We study whether a vision transformer can improve classification quality over a conventional CNN baseline on this task.\n\n\section{Method}\nOur model replaces the CNN feature extractor with a transformer encoder and keeps the same classifier head and training recipe as the baseline.\n\n\section{Results}\nTable~\ref{tab:main-results} reports the comparison between the CNN baseline and our transformer model.\n\n\begin{table}[h]\n\centering\n\caption{Classification accuracy on the held-out test set.}\n\label{tab:main-results}\n\begin{tabular}{lc}\n\toprule\nModel & Top-1 Accuracy \\\n\midrule\nCNN baseline & 89.4 \\\nTransformer & 92.1 \\\n\bottomrule\n\end{tabular}\n\end{table}\n\n\replaced{The transformer improves performance because global-context modeling reduces confusion between visually similar disease patterns while retaining discriminative lesion structure. The supporting evidence for this mechanism-based explanation is provided in the paragraph immediately below Table~2.}{The transformer model improves top-1 accuracy by 2.7 points over the CNN baseline.}\n\n\section{Conclusion}\nThe transformer model provides a simple accuracy improvement on this dataset.\n\n\end{document}\n | \documentclass{article}\n\usepackage{booktabs}\n\n\title{Transformer-Based Crop Disease Diagnosis}\n\author{Example Author}\n\date{}\n\n\begin{document}\n\maketitle\n\n\section{Introduction}\nCrop disease diagnosis from leaf images remains difficult when visually similar diseases appear under different lighting conditions. We study whether a vision transformer can improve classification quality over a conventional CNN baseline on this task.\n\n\section{Method}\nOur model replaces the CNN feature extractor with a transformer encoder and keeps the same classifier head and training recipe as the baseline.\n\n\section{Results}\nTable~\ref{tab:main-results} reports the comparison between the CNN baseline and our transformer model.\n\n\begin{table}[h]\n\centering\n\caption{Classification accuracy on the held-out test set.}\n\label{tab:main-results}\n\begin{tabular}{lc}\n\toprule\nModel & Top-1 Accuracy \\\n\midrule\nCNN baseline & 89.4 \\\nTransformer & 92.1 \\\n\bottomrule\n\end{tabular}\n\end{table}\n\nThe transformer improves performance because global-context modeling reduces confusion between visually similar disease patterns while retaining discriminative lesion structure. The supporting evidence for this mechanism-based explanation is provided in the paragraph immediately below Table~2.\n\n\section{Conclusion}\nThe transformer model provides a simple accuracy improvement on this dataset.\n\n\end{document}\n | Replace the full happy-path manuscript with the confirmed marked and clean variants. |

