# Response Letter Outline

这是从 `review-master.db` 渲染出的 Stage 6 预组装视图。数据库真源是 `raw_review_threads`、`response_thread_resolution_links`、`selected_action_copy_variants`、`response_thread_rows`、`atomic_comments`、`strategy_card_actions` 和 `strategy_action_target_locations`。最终 response letter 必须按原始 `thread_id` 顺序组织，而不是按 canonical atomic item 直接展开。这里的 thread-level row 来自 Stage 5 已确认的策略/草案、Stage 6 在各个 target location 上已选中的 manuscript 最终落稿文本，以及 thread-level 聚合；它不是从 response-side variants 中选出来的。

{% for group in reviewer_groups -%}
## {{ group.heading }}

{% for thread in group.threads -%}
### {{ thread.thread_id }}

- Original thread: {{ thread.original_text }}
- Normalized summary: {{ thread.normalized_summary }}
- Final row ready: {{ thread.row_ready }}

| Response Order | Role | `comment_id` | Canonical Summary | Selected Manuscript Copy Summary |
| --- | --- | --- | --- | --- |
{% for item in thread.resolutions -%}
| {{ item.response_order }} | {{ item.response_role }} | {{ item.comment_id }} | {{ item.canonical_summary }} | {{ item.selected_copy_summary }} |
{% else -%}
|  |  |  |  |  |
{% endfor %}

{% if thread.row_ready == "yes" -%}
#### Final Row Preview

| 原始意见 | 修改位置/范围 | 关键修改片段 | 回复说明 |
| --- | --- | --- | --- |
| {{ thread.final_row.original_comment }} | {{ thread.final_row.modification_scope }} | {{ thread.final_row.key_revision_excerpt }} | {{ thread.final_row.response_explanation }} |
{% endif %}

{% endfor -%}
{% else -%}
## No Response Threads

No response outline data available.
{% endfor %}
