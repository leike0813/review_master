# Export Patch Plan

这是从 `review-master.db` 渲染出的 Stage 6 导出补丁计划视图。数据库真源是 `export_patch_sets` 与 `export_patches`。导出脚本只负责复制导出副本，并按这里对应的显式锚点执行插入或替换；它不负责语义判断，也不负责生成最终文案。

## transformer_clean_patch_set

- 导出类型: clean_manuscript
- 来源根目录: /home/joshua/Workspace/Code/Skill/review_master/playbooks/examples/transformer-three-review-major-revision/inputs/manuscript
- 输出根目录: /home/joshua/Workspace/Code/Skill/review_master/playbooks/examples/transformer-three-review-major-revision/outputs/revised-manuscript
- 状态: exported

| 序号 | 意见 | 动作 | 位置 | 目标文件 | 操作 | 锚点文本 | Marked 文本 | Clean 文本 | 备注 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 1 | atomic_001 | 1 | 1 | sections/abstract.tex | replace | \begin{abstract}\nSequence transduction models are usually built from recurrent or convolutional components together with attention. We study a simpler architecture, the Transformer, that uses attention throughout the encoder and decoder. On WMT 2014 English-to-German and English-to-French translation, the model achieves stronger BLEU scores than competitive baselines while training faster on modern hardware. The results suggest that attention-centric architectures are a promising alternative for machine translation and possibly for other sequence modeling tasks.\n\end{abstract}\n | \begin{abstract}\nSequence transduction models are usually built from recurrent or convolutional components together with attention. We study a simpler architecture, the Transformer, that uses attention throughout the encoder and decoder. \added{Beyond improved parallelism, the key benefit is that self-attention shortens the path length between distant positions, allowing the model to exchange long-range information without recurrent state updates or stacked convolutional receptive fields.} On WMT 2014 English-to-German and English-to-French translation, the model achieves stronger BLEU scores than competitive baselines while training faster under the stated hardware and FLOPs accounting convention. \replaced{The results support attention-centric sequence transduction on these benchmarks, while leaving broader generalization claims to future work.}{The results suggest that attention-centric architectures are a promising alternative for machine translation and possibly for other sequence modeling tasks.}\n\end{abstract}\n | \begin{abstract}\nSequence transduction models are usually built from recurrent or convolutional components together with attention. We study a simpler architecture, the Transformer, that uses attention throughout the encoder and decoder. Beyond improved parallelism, the key benefit is that self-attention shortens the path length between distant positions, allowing the model to exchange long-range information without recurrent state updates or stacked convolutional receptive fields. On WMT 2014 English-to-German and English-to-French translation, the model achieves stronger BLEU scores than competitive baselines while training faster under the stated hardware and FLOPs accounting convention. The results support attention-centric sequence transduction on these benchmarks, while leaving broader generalization claims to future work.\n\end{abstract}\n | Replace sections/abstract.tex with the revised version for the runtime replay. |
| 2 | atomic_001 | 1 | 1 | sections/introduction.tex | replace | \section{Introduction}\n\nNeural machine translation systems have traditionally relied on recurrent computation, and more recent work has shown that convolution can also support strong sequence transduction quality. Attention mechanisms are now widely used in these systems because they improve the handling of distant dependencies and make encoder-decoder interaction easier to optimize.\n\nIn this paper we revisit the sequence transduction pipeline from the perspective of attention-first design. Instead of coupling attention with recurrence or convolution, we build a model in which the main computation is carried by stacked attention blocks and point-wise feed-forward layers. This leads to a system that is easier to parallelize and empirically stronger on large-scale translation benchmarks.\n\nOur experiments on WMT 2014 English-to-German and English-to-French show that the Transformer family delivers better headline BLEU scores than strong prior baselines at lower overall training cost. These findings indicate that attention can serve as a practical foundation for competitive translation systems.\n\nThe rest of the paper introduces the background, summarizes the model architecture, reports the training setup and main results, and closes with a brief discussion of broader implications.\n | \section{Introduction}\n\nNeural machine translation systems have traditionally relied on recurrent computation, and more recent work has shown that convolution can also support strong sequence transduction quality. Attention mechanisms are now widely used in these systems because they improve the handling of distant dependencies and make encoder-decoder interaction easier to optimize.\n\nIn this paper we revisit the sequence transduction pipeline from the perspective of attention-first design. Instead of coupling attention with recurrence or convolution, we build a model in which the main computation is carried by stacked attention blocks and point-wise feed-forward layers. This leads to a system that is easier to parallelize and empirically stronger on large-scale translation benchmarks.\n\n\added{The central intuition is not only throughput. Self-attention gives every token a short path to every other token, so long-range dependencies can be modeled without waiting for recurrent state propagation or deep convolutional stacks. In practice, this makes it easier to preserve long-distance syntactic and semantic cues while still exploiting efficient matrix computation on modern accelerators.}\n\nOur experiments on WMT 2014 English-to-German and English-to-French show that the Transformer family delivers better headline BLEU scores than strong prior baselines at lower overall training cost. These findings indicate that attention can serve as a practical foundation for competitive translation systems, while the following sections make clear where the current evidence is strongest and where it remains limited.\n | \section{Introduction}\n\nNeural machine translation systems have traditionally relied on recurrent computation, and more recent work has shown that convolution can also support strong sequence transduction quality. Attention mechanisms are now widely used in these systems because they improve the handling of distant dependencies and make encoder-decoder interaction easier to optimize.\n\nIn this paper we revisit the sequence transduction pipeline from the perspective of attention-first design. Instead of coupling attention with recurrence or convolution, we build a model in which the main computation is carried by stacked attention blocks and point-wise feed-forward layers. This leads to a system that is easier to parallelize and empirically stronger on large-scale translation benchmarks.\n\nThe central intuition is not only throughput. Self-attention gives every token a short path to every other token, so long-range dependencies can be modeled without waiting for recurrent state propagation or deep convolutional stacks. In practice, this makes it easier to preserve long-distance syntactic and semantic cues while still exploiting efficient matrix computation on modern accelerators.\n\nOur experiments on WMT 2014 English-to-German and English-to-French show that the Transformer family delivers better headline BLEU scores than strong prior baselines at lower overall training cost. These findings indicate that attention can serve as a practical foundation for competitive translation systems, while the following sections make clear where the current evidence is strongest and where it remains limited.\n | Replace sections/introduction.tex with the revised version for the runtime replay. |
| 3 | atomic_002 | 1 | 1 | sections/background.tex | replace | \section{Background}\n\nSeveral strands of prior work seek to reduce the sequential bottlenecks of recurrent sequence models. Convolutional approaches such as ByteNet and ConvS2S increase parallelism through shorter computation paths, while attention-based mechanisms and memory-style architectures improve access to context across positions.\n\nSelf-attention is especially attractive because it connects positions directly and can be implemented with matrix operations that fit well on modern accelerators. For sequence transduction, this suggests that attention-heavy models may achieve a better trade-off between quality and training efficiency than more sequential alternatives.\n\nWe therefore take the encoder-decoder structure as given and focus on replacing its recurrent or convolutional backbone with attention blocks. Our goal is not to review the full literature exhaustively, but to show that this design direction is practically competitive on large translation benchmarks.\n | \section{Background}\n\nSeveral strands of prior work seek to reduce the sequential bottlenecks of recurrent sequence models. Convolutional approaches such as ByteNet and ConvS2S increase parallelism through shorter computation paths, while attention-based mechanisms and memory-style architectures improve access to context across positions.\n\nSelf-attention is especially attractive because it connects positions directly and can be implemented with matrix operations that fit well on modern accelerators. For sequence transduction, this suggests that attention-heavy models may achieve a better trade-off between quality and training efficiency than more sequential alternatives.\n\n\added{Our novelty claim is therefore narrower than “attention is useful.” ByteNet and ConvS2S already show that efficient non-recurrent sequence models are possible, and memory-network style models already use attention as a central primitive. The specific claim here is that a strong encoder-decoder translation system can be built from self-attention and feed-forward blocks alone, without sequence-aligned recurrence or convolution in the main transduction backbone.}\n\nWe therefore take the encoder-decoder structure as given and focus on replacing its recurrent or convolutional backbone with attention blocks. The goal is not to review the full literature exhaustively, but to show that this design direction is practically competitive on large translation benchmarks and to clarify where it differs from prior efficient sequence models.\n | \section{Background}\n\nSeveral strands of prior work seek to reduce the sequential bottlenecks of recurrent sequence models. Convolutional approaches such as ByteNet and ConvS2S increase parallelism through shorter computation paths, while attention-based mechanisms and memory-style architectures improve access to context across positions.\n\nSelf-attention is especially attractive because it connects positions directly and can be implemented with matrix operations that fit well on modern accelerators. For sequence transduction, this suggests that attention-heavy models may achieve a better trade-off between quality and training efficiency than more sequential alternatives.\n\nOur novelty claim is therefore narrower than “attention is useful.” ByteNet and ConvS2S already show that efficient non-recurrent sequence models are possible, and memory-network style models already use attention as a central primitive. The specific claim here is that a strong encoder-decoder translation system can be built from self-attention and feed-forward blocks alone, without sequence-aligned recurrence or convolution in the main transduction backbone.\n\nWe therefore take the encoder-decoder structure as given and focus on replacing its recurrent or convolutional backbone with attention blocks. The goal is not to review the full literature exhaustively, but to show that this design direction is practically competitive on large translation benchmarks and to clarify where it differs from prior efficient sequence models.\n | Replace sections/background.tex with the revised version for the runtime replay. |
| 4 | atomic_003 | 1 | 1 | sections/architecture.tex | replace | \section{Model Architecture}\n\nThe Transformer follows the familiar encoder-decoder layout. The encoder maps an input sequence $(x_1, \ldots, x_n)$ to contextualized continuous representations, and the decoder generates the output sequence auto-regressively from those representations.\n\nEach encoder layer contains a multi-head self-attention sub-layer followed by a position-wise feed-forward network. The decoder mirrors this pattern and adds encoder-decoder attention between its self-attention block and feed-forward block. Residual connections and layer normalization are applied throughout.\n\n\subsection{Scaled Dot-Product Attention}\n\nGiven queries $Q$, keys $K$, and values $V$, we compute attention as\n\[\n\mathrm{Attention}(Q, K, V) = \mathrm{softmax}\left(\frac{QK^T}{\sqrt{d_k}}\right)V.\n\]\nThis formulation is efficient on GPUs and allows the model to relate all positions in a sequence without recurrence.\n\n\subsection{Multi-Head Attention and Positional Signals}\n\nInstead of using a single attention map, we project the inputs into multiple heads, compute attention in parallel, concatenate the resulting values, and apply a final linear projection. Positional information is injected by adding sinusoidal signals to the token embeddings at the bottoms of the encoder and decoder stacks.\n\n\subsection{Model Configuration}\n\nIn the experiments below we use a base configuration and a larger configuration. Both preserve the same overall architectural pattern, with the larger model increasing depth and width in a standard way. We found this family of settings to work well in practice, and use it for the headline comparisons reported later.\n | \section{Model Architecture}\n\nThe Transformer follows the familiar encoder-decoder layout. The encoder maps an input sequence $(x_1, \ldots, x_n)$ to contextualized continuous representations, and the decoder generates the output sequence auto-regressively from those representations.\n\nEach encoder layer contains a multi-head self-attention sub-layer followed by a position-wise feed-forward network. The decoder mirrors this pattern and adds encoder-decoder attention between its self-attention block and feed-forward block. Residual connections and layer normalization are applied throughout.\n\n\subsection{Scaled Dot-Product Attention}\n\nGiven queries $Q$, keys $K$, and values $V$, we compute attention as\n\[\n\mathrm{Attention}(Q, K, V) = \mathrm{softmax}\left(\frac{QK^T}{\sqrt{d_k}}\right)V.\n\]\nThis formulation is efficient on GPUs and allows the model to relate all positions in a sequence without recurrence.\n\n\subsection{Multi-Head Attention and Positional Signals}\n\nInstead of using a single attention map, we project the inputs into multiple heads, compute attention in parallel, concatenate the resulting values, and apply a final linear projection. Positional information is injected by adding sinusoidal signals to the token embeddings at the bottoms of the encoder and decoder stacks.\n\n\subsection{Model Configuration}\n\n\replaced{In the experiments below we use a base configuration and a larger configuration. The default setting uses six layers, eight heads, and a 512-dimensional model state for the base model because this regime balanced accuracy and efficiency well in our development runs. The accompanying variation study shows that the conclusions are not reducible to a single arbitrary hyperparameter choice: single-head attention underperforms, removing dropout hurts generalization, and larger models help without erasing the importance of the attention design itself.}{In the experiments below we use a base configuration and a larger configuration. Both preserve the same overall architectural pattern, with the larger model increasing depth and width in a standard way. We found this family of settings to work well in practice, and use it for the headline comparisons reported later.}\n | \section{Model Architecture}\n\nThe Transformer follows the familiar encoder-decoder layout. The encoder maps an input sequence $(x_1, \ldots, x_n)$ to contextualized continuous representations, and the decoder generates the output sequence auto-regressively from those representations.\n\nEach encoder layer contains a multi-head self-attention sub-layer followed by a position-wise feed-forward network. The decoder mirrors this pattern and adds encoder-decoder attention between its self-attention block and feed-forward block. Residual connections and layer normalization are applied throughout.\n\n\subsection{Scaled Dot-Product Attention}\n\nGiven queries $Q$, keys $K$, and values $V$, we compute attention as\n\[\n\mathrm{Attention}(Q, K, V) = \mathrm{softmax}\left(\frac{QK^T}{\sqrt{d_k}}\right)V.\n\]\nThis formulation is efficient on GPUs and allows the model to relate all positions in a sequence without recurrence.\n\n\subsection{Multi-Head Attention and Positional Signals}\n\nInstead of using a single attention map, we project the inputs into multiple heads, compute attention in parallel, concatenate the resulting values, and apply a final linear projection. Positional information is injected by adding sinusoidal signals to the token embeddings at the bottoms of the encoder and decoder stacks.\n\n\subsection{Model Configuration}\n\nIn the experiments below we use a base configuration and a larger configuration. The default setting uses six layers, eight heads, and a 512-dimensional model state for the base model because this regime balanced accuracy and efficiency well in our development runs. The accompanying variation study shows that the conclusions are not reducible to a single arbitrary hyperparameter choice: single-head attention underperforms, removing dropout hurts generalization, and larger models help without erasing the importance of the attention design itself.\n | Replace sections/architecture.tex with the revised version for the runtime replay. |
| 5 | atomic_005 | 1 | 1 | sections/training.tex | replace | \section{Training}\n\n\subsection{Data}\n\nWe train on the standard WMT 2014 English-to-German and English-to-French translation tasks. Source and target sentences are tokenized with standard subword segmentation and batched by approximate sequence length.\n\n\subsection{Optimization}\n\nWe optimize the models with Adam and use a learning-rate schedule with an initial warmup phase followed by inverse-square-root decay. Regularization includes dropout and label smoothing. Decoding uses beam search.\n\n\subsection{Implementation Notes}\n\nAll models are trained on multi-GPU hardware, and the reported results come from configurations that were stable in our experiments. We use common model-selection practices from the sequence-to-sequence literature and report the strongest checkpoints obtained during training.\n | \section{Training}\n\n\subsection{Data}\n\n\replaced{We train on the standard WMT 2014 English-to-German and English-to-French translation tasks. English-German uses approximately 4.5M sentence pairs after standard cleaning and shared byte-pair encoding with a vocabulary of roughly 37k tokens. English-French uses approximately 36M sentence pairs with a 32k word-piece vocabulary. Sentence pairs are batched by approximate sequence length.}{We train on the standard WMT 2014 English-to-German and English-to-French translation tasks. Source and target sentences are tokenized with standard subword segmentation and batched by approximate sequence length.}\n\n\subsection{Optimization}\n\nWe optimize the models with Adam and use a learning-rate schedule with an initial warmup phase followed by inverse-square-root decay. Regularization includes dropout and label smoothing. Decoding uses beam search.\n\n\subsection{Implementation Notes}\n\n\replaced{All models are trained on 8 NVIDIA P100 GPUs. The base model is reported after averaging the final 5 checkpoints written at 10-minute intervals, while the big model averages the final 20 checkpoints. During inference we use beam size 4, length penalty 0.6, and a maximum output length of input length plus 50 with early stopping when possible. These details are necessary for a reproducible interpretation of the reported benchmark results.}{All models are trained on multi-GPU hardware, and the reported results come from configurations that were stable in our experiments. We use common model-selection practices from the sequence-to-sequence literature and report the strongest checkpoints obtained during training.}\n | \section{Training}\n\n\subsection{Data}\n\nWe train on the standard WMT 2014 English-to-German and English-to-French translation tasks. English-German uses approximately 4.5M sentence pairs after standard cleaning and shared byte-pair encoding with a vocabulary of roughly 37k tokens. English-French uses approximately 36M sentence pairs with a 32k word-piece vocabulary. Sentence pairs are batched by approximate sequence length.\n\n\subsection{Optimization}\n\nWe optimize the models with Adam and use a learning-rate schedule with an initial warmup phase followed by inverse-square-root decay. Regularization includes dropout and label smoothing. Decoding uses beam search.\n\n\subsection{Implementation Notes}\n\nAll models are trained on 8 NVIDIA P100 GPUs. The base model is reported after averaging the final 5 checkpoints written at 10-minute intervals, while the big model averages the final 20 checkpoints. During inference we use beam size 4, length penalty 0.6, and a maximum output length of input length plus 50 with early stopping when possible. These details are necessary for a reproducible interpretation of the reported benchmark results.\n | Replace sections/training.tex with the revised version for the runtime replay. |
| 6 | atomic_007 | 1 | 1 | sections/results.tex | replace | \section{Results}\n\n\subsection{Machine Translation}\n\nThe Transformer improves over strong prior baselines on both WMT 2014 English-to-German and English-to-French. The larger model achieves a new state of the art on English-to-German and remains highly competitive on English-to-French while requiring substantially less training cost than earlier recurrent or convolutional systems.\n\n\begin{table}[h]\n\centering\n\caption{Headline comparison on WMT 2014 translation benchmarks.}\n\label{tab:headline-results}\n\begin{tabular}{lcc}\n\toprule\nModel & EN-DE BLEU & EN-FR BLEU \\\n\midrule\nGNMT + RL & 24.6 & 39.92 \\\nConvS2S & 25.16 & 40.46 \\\nTransformer (base) & 27.3 & 38.1 \\\nTransformer (big) & 28.4 & 41.0 \\\n\bottomrule\n\end{tabular}\n\end{table}\n\nThese results indicate that the attention-centric formulation is effective in practice. In particular, the model appears to capture long-range dependencies while remaining easy to optimize in parallel.\n\n\subsection{Model Variations}\n\nWe also tested several variations of the Transformer family during development. The overall pattern was consistent with the headline results: larger attention-based models performed better, and the architecture was robust enough to support a strong translation system across both benchmark settings.\n\n\subsection{Interpretability}\n\nQualitatively, some attention heads appear to focus on linguistically meaningful relationships. This suggests that the model may also offer a more interpretable view of sequence transduction behavior than purely recurrent alternatives.\n | \section{Results}\n\n\subsection{Machine Translation}\n\n\replaced{The Transformer improves over strong prior baselines on both WMT 2014 English-to-German and English-to-French. The larger model achieves a new state of the art on English-to-German and remains highly competitive on English-to-French while requiring substantially less reported training cost than earlier recurrent or convolutional systems under the stated accounting convention.}{The Transformer improves over strong prior baselines on both WMT 2014 English-to-German and English-to-French. The larger model achieves a new state of the art on English-to-German and remains highly competitive on English-to-French while requiring substantially less training cost than earlier recurrent or convolutional systems.}\n\n\begin{table}[h]\n\centering\n\caption{Headline comparison on WMT 2014 translation benchmarks.}\n\label{tab:headline-results}\n\begin{tabular}{lcc}\n\toprule\nModel & EN-DE BLEU & EN-FR BLEU \\\n\midrule\nGNMT + RL & 24.6 & 39.92 \\\nConvS2S & 25.16 & 40.46 \\\nTransformer (base) & 27.3 & 38.1 \\\nTransformer (big) & 28.4 & 41.0 \\\n\bottomrule\n\end{tabular}\n\end{table}\n\n\added{The baseline gain is not only a throughput story. In the main comparison, the attention-only encoder-decoder appears to benefit from short cross-token path lengths, which helps preserve long-range dependencies without recurrent state propagation. This gives a plausible explanation for why the model improves over baselines that remain more sequential in how they transport context.}\n\n\subsection{Model Variations}\n\n\replaced{We also tested several variations of the Transformer family during development. Single-head attention underperformed the default multi-head setting, removing dropout reduced generalization quality, and scaling the model improved BLEU without eliminating the contribution of the attention design. These patterns support the claim that the gains are not explained by model size alone.}{We also tested several variations of the Transformer family during development. The overall pattern was consistent with the headline results: larger attention-based models performed better, and the architecture was robust enough to support a strong translation system across both benchmark settings.}\n\n\subsection{Stability Statement}\n\n\added{We do not claim a full variance analysis from the draft alone, but the supplemental five-run evidence shows a narrow spread around the main result. This supports a conservative stability statement: the headline BLEU improvements are not obviously driven by a single unstable run, although broader robustness analysis remains future work.}\n\n\subsection{Interpretability}\n\n\replaced{Qualitatively, some attention heads appear to focus on linguistically meaningful relationships. In one long English-German sentence, an upper-layer head links a subordinate-clause introducer to the postponed main verb, while another head tracks a named entity span across punctuation boundaries. We treat this as an illustrative case study rather than a complete explanation of model behavior.}{Qualitatively, some attention heads appear to focus on linguistically meaningful relationships. This suggests that the model may also offer a more interpretable view of sequence transduction behavior than purely recurrent alternatives.}\n | \section{Results}\n\n\subsection{Machine Translation}\n\nThe Transformer improves over strong prior baselines on both WMT 2014 English-to-German and English-to-French. The larger model achieves a new state of the art on English-to-German and remains highly competitive on English-to-French while requiring substantially less reported training cost than earlier recurrent or convolutional systems under the stated accounting convention.\n\n\begin{table}[h]\n\centering\n\caption{Headline comparison on WMT 2014 translation benchmarks.}\n\label{tab:headline-results}\n\begin{tabular}{lcc}\n\toprule\nModel & EN-DE BLEU & EN-FR BLEU \\\n\midrule\nGNMT + RL & 24.6 & 39.92 \\\nConvS2S & 25.16 & 40.46 \\\nTransformer (base) & 27.3 & 38.1 \\\nTransformer (big) & 28.4 & 41.0 \\\n\bottomrule\n\end{tabular}\n\end{table}\n\nThe baseline gain is not only a throughput story. In the main comparison, the attention-only encoder-decoder appears to benefit from short cross-token path lengths, which helps preserve long-range dependencies without recurrent state propagation. This gives a plausible explanation for why the model improves over baselines that remain more sequential in how they transport context.\n\n\subsection{Model Variations}\n\nWe also tested several variations of the Transformer family during development. Single-head attention underperformed the default multi-head setting, removing dropout reduced generalization quality, and scaling the model improved BLEU without eliminating the contribution of the attention design. These patterns support the claim that the gains are not explained by model size alone.\n\n\subsection{Stability Statement}\n\nWe do not claim a full variance analysis from the draft alone, but the supplemental five-run evidence shows a narrow spread around the main result. This supports a conservative stability statement: the headline BLEU improvements are not obviously driven by a single unstable run, although broader robustness analysis remains future work.\n\n\subsection{Interpretability}\n\nQualitatively, some attention heads appear to focus on linguistically meaningful relationships. In one long English-German sentence, an upper-layer head links a subordinate-clause introducer to the postponed main verb, while another head tracks a named entity span across punctuation boundaries. We treat this as an illustrative case study rather than a complete explanation of model behavior.\n | Replace sections/results.tex with the revised version for the runtime replay. |
| 7 | atomic_008 | 1 | 1 | sections/discussion.tex | replace | \section{Discussion}\n\nThe main takeaway from these experiments is that attention can serve as the primary computational primitive in a strong translation model. The resulting system trains efficiently and produces excellent benchmark performance.\n\nAt the same time, there are still practical questions around scaling, evaluation breadth, and behavior outside the current benchmark setting. These questions are important directions for future work, but they do not change the main empirical finding that attention-first sequence transduction is viable.\n | \section{Discussion}\n\nThe main takeaway from these experiments is that attention can serve as the primary computational primitive in a strong translation model. The resulting system trains efficiently and produces excellent benchmark performance.\n\n\added{The evidence is still bounded. The current results are tied to WMT 2014 machine translation and do not by themselves establish that self-attention is universally preferable for every sequence task.}\n\n\added{Failure analysis also remains important. Rare words, long subordinate-clause constructions, and some coverage-sensitive cases still account for a meaningful share of residual errors, even though aggregate BLEU improves over prior systems.}\n\n\replaced{Finally, self-attention offers short information paths but can become expensive for very long inputs. The qualitative attention examples are informative, yet they should be interpreted as illustrative analysis rather than definitive mechanistic proof.}{At the same time, there are still practical questions around scaling, evaluation breadth, and behavior outside the current benchmark setting. These questions are important directions for future work, but they do not change the main empirical finding that attention-first sequence transduction is viable.}\n | \section{Discussion}\n\nThe main takeaway from these experiments is that attention can serve as the primary computational primitive in a strong translation model. The resulting system trains efficiently and produces excellent benchmark performance.\n\nThe evidence is still bounded. The current results are tied to WMT 2014 machine translation and do not by themselves establish that self-attention is universally preferable for every sequence task.\n\nFailure analysis also remains important. Rare words, long subordinate-clause constructions, and some coverage-sensitive cases still account for a meaningful share of residual errors, even though aggregate BLEU improves over prior systems.\n\nFinally, self-attention offers short information paths but can become expensive for very long inputs. The qualitative attention examples are informative, yet they should be interpreted as illustrative analysis rather than definitive mechanistic proof.\n | Replace sections/discussion.tex with the revised version for the runtime replay. |
| 8 | atomic_008 | 1 | 1 | sections/conclusion.tex | replace | \section{Conclusion}\n\nWe introduced the Transformer, an attention-based sequence transduction model that removes recurrence and convolution from the core architecture. Across standard machine translation benchmarks, the model reaches state-of-the-art quality with strong computational efficiency.\n\nThese findings suggest that attention-based modeling may become a general foundation for many sequence-processing tasks beyond the translation setting studied here. We expect this line of work to remain broadly useful as larger datasets and broader modalities are considered.\n | \section{Conclusion}\n\nWe introduced the Transformer, an attention-based sequence transduction model that removes recurrence and convolution from the core architecture. Across standard machine translation benchmarks, the model reaches state-of-the-art quality with strong computational efficiency under the reported hardware and cost assumptions.\n\n\replaced{These findings suggest that attention-based modeling is a strong foundation for large-scale translation. Claims beyond that scope should be made cautiously: the present evidence is strongest for the benchmark tasks studied here, and broader modality or sequence-length generalization remains future work.}{These findings suggest that attention-based modeling may become a general foundation for many sequence-processing tasks beyond the translation setting studied here. We expect this line of work to remain broadly useful as larger datasets and broader modalities are considered.}\n | \section{Conclusion}\n\nWe introduced the Transformer, an attention-based sequence transduction model that removes recurrence and convolution from the core architecture. Across standard machine translation benchmarks, the model reaches state-of-the-art quality with strong computational efficiency under the reported hardware and cost assumptions.\n\nThese findings suggest that attention-based modeling is a strong foundation for large-scale translation. Claims beyond that scope should be made cautiously: the present evidence is strongest for the benchmark tasks studied here, and broader modality or sequence-length generalization remains future work.\n | Replace sections/conclusion.tex with the revised version for the runtime replay. |

## transformer_marked_patch_set

- 导出类型: marked_manuscript
- 来源根目录: /home/joshua/Workspace/Code/Skill/review_master/playbooks/examples/transformer-three-review-major-revision/inputs/manuscript
- 输出根目录: /home/joshua/Workspace/Code/Skill/review_master/playbooks/examples/transformer-three-review-major-revision/outputs/marked-manuscript
- 状态: exported

| 序号 | 意见 | 动作 | 位置 | 目标文件 | 操作 | 锚点文本 | Marked 文本 | Clean 文本 | 备注 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 1 | atomic_001 | 1 | 1 | sections/abstract.tex | replace | \begin{abstract}\nSequence transduction models are usually built from recurrent or convolutional components together with attention. We study a simpler architecture, the Transformer, that uses attention throughout the encoder and decoder. On WMT 2014 English-to-German and English-to-French translation, the model achieves stronger BLEU scores than competitive baselines while training faster on modern hardware. The results suggest that attention-centric architectures are a promising alternative for machine translation and possibly for other sequence modeling tasks.\n\end{abstract}\n | \begin{abstract}\nSequence transduction models are usually built from recurrent or convolutional components together with attention. We study a simpler architecture, the Transformer, that uses attention throughout the encoder and decoder. \added{Beyond improved parallelism, the key benefit is that self-attention shortens the path length between distant positions, allowing the model to exchange long-range information without recurrent state updates or stacked convolutional receptive fields.} On WMT 2014 English-to-German and English-to-French translation, the model achieves stronger BLEU scores than competitive baselines while training faster under the stated hardware and FLOPs accounting convention. \replaced{The results support attention-centric sequence transduction on these benchmarks, while leaving broader generalization claims to future work.}{The results suggest that attention-centric architectures are a promising alternative for machine translation and possibly for other sequence modeling tasks.}\n\end{abstract}\n | \begin{abstract}\nSequence transduction models are usually built from recurrent or convolutional components together with attention. We study a simpler architecture, the Transformer, that uses attention throughout the encoder and decoder. Beyond improved parallelism, the key benefit is that self-attention shortens the path length between distant positions, allowing the model to exchange long-range information without recurrent state updates or stacked convolutional receptive fields. On WMT 2014 English-to-German and English-to-French translation, the model achieves stronger BLEU scores than competitive baselines while training faster under the stated hardware and FLOPs accounting convention. The results support attention-centric sequence transduction on these benchmarks, while leaving broader generalization claims to future work.\n\end{abstract}\n | Replace sections/abstract.tex with the revised version for the runtime replay. |
| 2 | atomic_001 | 1 | 1 | sections/introduction.tex | replace | \section{Introduction}\n\nNeural machine translation systems have traditionally relied on recurrent computation, and more recent work has shown that convolution can also support strong sequence transduction quality. Attention mechanisms are now widely used in these systems because they improve the handling of distant dependencies and make encoder-decoder interaction easier to optimize.\n\nIn this paper we revisit the sequence transduction pipeline from the perspective of attention-first design. Instead of coupling attention with recurrence or convolution, we build a model in which the main computation is carried by stacked attention blocks and point-wise feed-forward layers. This leads to a system that is easier to parallelize and empirically stronger on large-scale translation benchmarks.\n\nOur experiments on WMT 2014 English-to-German and English-to-French show that the Transformer family delivers better headline BLEU scores than strong prior baselines at lower overall training cost. These findings indicate that attention can serve as a practical foundation for competitive translation systems.\n\nThe rest of the paper introduces the background, summarizes the model architecture, reports the training setup and main results, and closes with a brief discussion of broader implications.\n | \section{Introduction}\n\nNeural machine translation systems have traditionally relied on recurrent computation, and more recent work has shown that convolution can also support strong sequence transduction quality. Attention mechanisms are now widely used in these systems because they improve the handling of distant dependencies and make encoder-decoder interaction easier to optimize.\n\nIn this paper we revisit the sequence transduction pipeline from the perspective of attention-first design. Instead of coupling attention with recurrence or convolution, we build a model in which the main computation is carried by stacked attention blocks and point-wise feed-forward layers. This leads to a system that is easier to parallelize and empirically stronger on large-scale translation benchmarks.\n\n\added{The central intuition is not only throughput. Self-attention gives every token a short path to every other token, so long-range dependencies can be modeled without waiting for recurrent state propagation or deep convolutional stacks. In practice, this makes it easier to preserve long-distance syntactic and semantic cues while still exploiting efficient matrix computation on modern accelerators.}\n\nOur experiments on WMT 2014 English-to-German and English-to-French show that the Transformer family delivers better headline BLEU scores than strong prior baselines at lower overall training cost. These findings indicate that attention can serve as a practical foundation for competitive translation systems, while the following sections make clear where the current evidence is strongest and where it remains limited.\n | \section{Introduction}\n\nNeural machine translation systems have traditionally relied on recurrent computation, and more recent work has shown that convolution can also support strong sequence transduction quality. Attention mechanisms are now widely used in these systems because they improve the handling of distant dependencies and make encoder-decoder interaction easier to optimize.\n\nIn this paper we revisit the sequence transduction pipeline from the perspective of attention-first design. Instead of coupling attention with recurrence or convolution, we build a model in which the main computation is carried by stacked attention blocks and point-wise feed-forward layers. This leads to a system that is easier to parallelize and empirically stronger on large-scale translation benchmarks.\n\nThe central intuition is not only throughput. Self-attention gives every token a short path to every other token, so long-range dependencies can be modeled without waiting for recurrent state propagation or deep convolutional stacks. In practice, this makes it easier to preserve long-distance syntactic and semantic cues while still exploiting efficient matrix computation on modern accelerators.\n\nOur experiments on WMT 2014 English-to-German and English-to-French show that the Transformer family delivers better headline BLEU scores than strong prior baselines at lower overall training cost. These findings indicate that attention can serve as a practical foundation for competitive translation systems, while the following sections make clear where the current evidence is strongest and where it remains limited.\n | Replace sections/introduction.tex with the revised version for the runtime replay. |
| 3 | atomic_002 | 1 | 1 | sections/background.tex | replace | \section{Background}\n\nSeveral strands of prior work seek to reduce the sequential bottlenecks of recurrent sequence models. Convolutional approaches such as ByteNet and ConvS2S increase parallelism through shorter computation paths, while attention-based mechanisms and memory-style architectures improve access to context across positions.\n\nSelf-attention is especially attractive because it connects positions directly and can be implemented with matrix operations that fit well on modern accelerators. For sequence transduction, this suggests that attention-heavy models may achieve a better trade-off between quality and training efficiency than more sequential alternatives.\n\nWe therefore take the encoder-decoder structure as given and focus on replacing its recurrent or convolutional backbone with attention blocks. Our goal is not to review the full literature exhaustively, but to show that this design direction is practically competitive on large translation benchmarks.\n | \section{Background}\n\nSeveral strands of prior work seek to reduce the sequential bottlenecks of recurrent sequence models. Convolutional approaches such as ByteNet and ConvS2S increase parallelism through shorter computation paths, while attention-based mechanisms and memory-style architectures improve access to context across positions.\n\nSelf-attention is especially attractive because it connects positions directly and can be implemented with matrix operations that fit well on modern accelerators. For sequence transduction, this suggests that attention-heavy models may achieve a better trade-off between quality and training efficiency than more sequential alternatives.\n\n\added{Our novelty claim is therefore narrower than “attention is useful.” ByteNet and ConvS2S already show that efficient non-recurrent sequence models are possible, and memory-network style models already use attention as a central primitive. The specific claim here is that a strong encoder-decoder translation system can be built from self-attention and feed-forward blocks alone, without sequence-aligned recurrence or convolution in the main transduction backbone.}\n\nWe therefore take the encoder-decoder structure as given and focus on replacing its recurrent or convolutional backbone with attention blocks. The goal is not to review the full literature exhaustively, but to show that this design direction is practically competitive on large translation benchmarks and to clarify where it differs from prior efficient sequence models.\n | \section{Background}\n\nSeveral strands of prior work seek to reduce the sequential bottlenecks of recurrent sequence models. Convolutional approaches such as ByteNet and ConvS2S increase parallelism through shorter computation paths, while attention-based mechanisms and memory-style architectures improve access to context across positions.\n\nSelf-attention is especially attractive because it connects positions directly and can be implemented with matrix operations that fit well on modern accelerators. For sequence transduction, this suggests that attention-heavy models may achieve a better trade-off between quality and training efficiency than more sequential alternatives.\n\nOur novelty claim is therefore narrower than “attention is useful.” ByteNet and ConvS2S already show that efficient non-recurrent sequence models are possible, and memory-network style models already use attention as a central primitive. The specific claim here is that a strong encoder-decoder translation system can be built from self-attention and feed-forward blocks alone, without sequence-aligned recurrence or convolution in the main transduction backbone.\n\nWe therefore take the encoder-decoder structure as given and focus on replacing its recurrent or convolutional backbone with attention blocks. The goal is not to review the full literature exhaustively, but to show that this design direction is practically competitive on large translation benchmarks and to clarify where it differs from prior efficient sequence models.\n | Replace sections/background.tex with the revised version for the runtime replay. |
| 4 | atomic_003 | 1 | 1 | sections/architecture.tex | replace | \section{Model Architecture}\n\nThe Transformer follows the familiar encoder-decoder layout. The encoder maps an input sequence $(x_1, \ldots, x_n)$ to contextualized continuous representations, and the decoder generates the output sequence auto-regressively from those representations.\n\nEach encoder layer contains a multi-head self-attention sub-layer followed by a position-wise feed-forward network. The decoder mirrors this pattern and adds encoder-decoder attention between its self-attention block and feed-forward block. Residual connections and layer normalization are applied throughout.\n\n\subsection{Scaled Dot-Product Attention}\n\nGiven queries $Q$, keys $K$, and values $V$, we compute attention as\n\[\n\mathrm{Attention}(Q, K, V) = \mathrm{softmax}\left(\frac{QK^T}{\sqrt{d_k}}\right)V.\n\]\nThis formulation is efficient on GPUs and allows the model to relate all positions in a sequence without recurrence.\n\n\subsection{Multi-Head Attention and Positional Signals}\n\nInstead of using a single attention map, we project the inputs into multiple heads, compute attention in parallel, concatenate the resulting values, and apply a final linear projection. Positional information is injected by adding sinusoidal signals to the token embeddings at the bottoms of the encoder and decoder stacks.\n\n\subsection{Model Configuration}\n\nIn the experiments below we use a base configuration and a larger configuration. Both preserve the same overall architectural pattern, with the larger model increasing depth and width in a standard way. We found this family of settings to work well in practice, and use it for the headline comparisons reported later.\n | \section{Model Architecture}\n\nThe Transformer follows the familiar encoder-decoder layout. The encoder maps an input sequence $(x_1, \ldots, x_n)$ to contextualized continuous representations, and the decoder generates the output sequence auto-regressively from those representations.\n\nEach encoder layer contains a multi-head self-attention sub-layer followed by a position-wise feed-forward network. The decoder mirrors this pattern and adds encoder-decoder attention between its self-attention block and feed-forward block. Residual connections and layer normalization are applied throughout.\n\n\subsection{Scaled Dot-Product Attention}\n\nGiven queries $Q$, keys $K$, and values $V$, we compute attention as\n\[\n\mathrm{Attention}(Q, K, V) = \mathrm{softmax}\left(\frac{QK^T}{\sqrt{d_k}}\right)V.\n\]\nThis formulation is efficient on GPUs and allows the model to relate all positions in a sequence without recurrence.\n\n\subsection{Multi-Head Attention and Positional Signals}\n\nInstead of using a single attention map, we project the inputs into multiple heads, compute attention in parallel, concatenate the resulting values, and apply a final linear projection. Positional information is injected by adding sinusoidal signals to the token embeddings at the bottoms of the encoder and decoder stacks.\n\n\subsection{Model Configuration}\n\n\replaced{In the experiments below we use a base configuration and a larger configuration. The default setting uses six layers, eight heads, and a 512-dimensional model state for the base model because this regime balanced accuracy and efficiency well in our development runs. The accompanying variation study shows that the conclusions are not reducible to a single arbitrary hyperparameter choice: single-head attention underperforms, removing dropout hurts generalization, and larger models help without erasing the importance of the attention design itself.}{In the experiments below we use a base configuration and a larger configuration. Both preserve the same overall architectural pattern, with the larger model increasing depth and width in a standard way. We found this family of settings to work well in practice, and use it for the headline comparisons reported later.}\n | \section{Model Architecture}\n\nThe Transformer follows the familiar encoder-decoder layout. The encoder maps an input sequence $(x_1, \ldots, x_n)$ to contextualized continuous representations, and the decoder generates the output sequence auto-regressively from those representations.\n\nEach encoder layer contains a multi-head self-attention sub-layer followed by a position-wise feed-forward network. The decoder mirrors this pattern and adds encoder-decoder attention between its self-attention block and feed-forward block. Residual connections and layer normalization are applied throughout.\n\n\subsection{Scaled Dot-Product Attention}\n\nGiven queries $Q$, keys $K$, and values $V$, we compute attention as\n\[\n\mathrm{Attention}(Q, K, V) = \mathrm{softmax}\left(\frac{QK^T}{\sqrt{d_k}}\right)V.\n\]\nThis formulation is efficient on GPUs and allows the model to relate all positions in a sequence without recurrence.\n\n\subsection{Multi-Head Attention and Positional Signals}\n\nInstead of using a single attention map, we project the inputs into multiple heads, compute attention in parallel, concatenate the resulting values, and apply a final linear projection. Positional information is injected by adding sinusoidal signals to the token embeddings at the bottoms of the encoder and decoder stacks.\n\n\subsection{Model Configuration}\n\nIn the experiments below we use a base configuration and a larger configuration. The default setting uses six layers, eight heads, and a 512-dimensional model state for the base model because this regime balanced accuracy and efficiency well in our development runs. The accompanying variation study shows that the conclusions are not reducible to a single arbitrary hyperparameter choice: single-head attention underperforms, removing dropout hurts generalization, and larger models help without erasing the importance of the attention design itself.\n | Replace sections/architecture.tex with the revised version for the runtime replay. |
| 5 | atomic_005 | 1 | 1 | sections/training.tex | replace | \section{Training}\n\n\subsection{Data}\n\nWe train on the standard WMT 2014 English-to-German and English-to-French translation tasks. Source and target sentences are tokenized with standard subword segmentation and batched by approximate sequence length.\n\n\subsection{Optimization}\n\nWe optimize the models with Adam and use a learning-rate schedule with an initial warmup phase followed by inverse-square-root decay. Regularization includes dropout and label smoothing. Decoding uses beam search.\n\n\subsection{Implementation Notes}\n\nAll models are trained on multi-GPU hardware, and the reported results come from configurations that were stable in our experiments. We use common model-selection practices from the sequence-to-sequence literature and report the strongest checkpoints obtained during training.\n | \section{Training}\n\n\subsection{Data}\n\n\replaced{We train on the standard WMT 2014 English-to-German and English-to-French translation tasks. English-German uses approximately 4.5M sentence pairs after standard cleaning and shared byte-pair encoding with a vocabulary of roughly 37k tokens. English-French uses approximately 36M sentence pairs with a 32k word-piece vocabulary. Sentence pairs are batched by approximate sequence length.}{We train on the standard WMT 2014 English-to-German and English-to-French translation tasks. Source and target sentences are tokenized with standard subword segmentation and batched by approximate sequence length.}\n\n\subsection{Optimization}\n\nWe optimize the models with Adam and use a learning-rate schedule with an initial warmup phase followed by inverse-square-root decay. Regularization includes dropout and label smoothing. Decoding uses beam search.\n\n\subsection{Implementation Notes}\n\n\replaced{All models are trained on 8 NVIDIA P100 GPUs. The base model is reported after averaging the final 5 checkpoints written at 10-minute intervals, while the big model averages the final 20 checkpoints. During inference we use beam size 4, length penalty 0.6, and a maximum output length of input length plus 50 with early stopping when possible. These details are necessary for a reproducible interpretation of the reported benchmark results.}{All models are trained on multi-GPU hardware, and the reported results come from configurations that were stable in our experiments. We use common model-selection practices from the sequence-to-sequence literature and report the strongest checkpoints obtained during training.}\n | \section{Training}\n\n\subsection{Data}\n\nWe train on the standard WMT 2014 English-to-German and English-to-French translation tasks. English-German uses approximately 4.5M sentence pairs after standard cleaning and shared byte-pair encoding with a vocabulary of roughly 37k tokens. English-French uses approximately 36M sentence pairs with a 32k word-piece vocabulary. Sentence pairs are batched by approximate sequence length.\n\n\subsection{Optimization}\n\nWe optimize the models with Adam and use a learning-rate schedule with an initial warmup phase followed by inverse-square-root decay. Regularization includes dropout and label smoothing. Decoding uses beam search.\n\n\subsection{Implementation Notes}\n\nAll models are trained on 8 NVIDIA P100 GPUs. The base model is reported after averaging the final 5 checkpoints written at 10-minute intervals, while the big model averages the final 20 checkpoints. During inference we use beam size 4, length penalty 0.6, and a maximum output length of input length plus 50 with early stopping when possible. These details are necessary for a reproducible interpretation of the reported benchmark results.\n | Replace sections/training.tex with the revised version for the runtime replay. |
| 6 | atomic_007 | 1 | 1 | sections/results.tex | replace | \section{Results}\n\n\subsection{Machine Translation}\n\nThe Transformer improves over strong prior baselines on both WMT 2014 English-to-German and English-to-French. The larger model achieves a new state of the art on English-to-German and remains highly competitive on English-to-French while requiring substantially less training cost than earlier recurrent or convolutional systems.\n\n\begin{table}[h]\n\centering\n\caption{Headline comparison on WMT 2014 translation benchmarks.}\n\label{tab:headline-results}\n\begin{tabular}{lcc}\n\toprule\nModel & EN-DE BLEU & EN-FR BLEU \\\n\midrule\nGNMT + RL & 24.6 & 39.92 \\\nConvS2S & 25.16 & 40.46 \\\nTransformer (base) & 27.3 & 38.1 \\\nTransformer (big) & 28.4 & 41.0 \\\n\bottomrule\n\end{tabular}\n\end{table}\n\nThese results indicate that the attention-centric formulation is effective in practice. In particular, the model appears to capture long-range dependencies while remaining easy to optimize in parallel.\n\n\subsection{Model Variations}\n\nWe also tested several variations of the Transformer family during development. The overall pattern was consistent with the headline results: larger attention-based models performed better, and the architecture was robust enough to support a strong translation system across both benchmark settings.\n\n\subsection{Interpretability}\n\nQualitatively, some attention heads appear to focus on linguistically meaningful relationships. This suggests that the model may also offer a more interpretable view of sequence transduction behavior than purely recurrent alternatives.\n | \section{Results}\n\n\subsection{Machine Translation}\n\n\replaced{The Transformer improves over strong prior baselines on both WMT 2014 English-to-German and English-to-French. The larger model achieves a new state of the art on English-to-German and remains highly competitive on English-to-French while requiring substantially less reported training cost than earlier recurrent or convolutional systems under the stated accounting convention.}{The Transformer improves over strong prior baselines on both WMT 2014 English-to-German and English-to-French. The larger model achieves a new state of the art on English-to-German and remains highly competitive on English-to-French while requiring substantially less training cost than earlier recurrent or convolutional systems.}\n\n\begin{table}[h]\n\centering\n\caption{Headline comparison on WMT 2014 translation benchmarks.}\n\label{tab:headline-results}\n\begin{tabular}{lcc}\n\toprule\nModel & EN-DE BLEU & EN-FR BLEU \\\n\midrule\nGNMT + RL & 24.6 & 39.92 \\\nConvS2S & 25.16 & 40.46 \\\nTransformer (base) & 27.3 & 38.1 \\\nTransformer (big) & 28.4 & 41.0 \\\n\bottomrule\n\end{tabular}\n\end{table}\n\n\added{The baseline gain is not only a throughput story. In the main comparison, the attention-only encoder-decoder appears to benefit from short cross-token path lengths, which helps preserve long-range dependencies without recurrent state propagation. This gives a plausible explanation for why the model improves over baselines that remain more sequential in how they transport context.}\n\n\subsection{Model Variations}\n\n\replaced{We also tested several variations of the Transformer family during development. Single-head attention underperformed the default multi-head setting, removing dropout reduced generalization quality, and scaling the model improved BLEU without eliminating the contribution of the attention design. These patterns support the claim that the gains are not explained by model size alone.}{We also tested several variations of the Transformer family during development. The overall pattern was consistent with the headline results: larger attention-based models performed better, and the architecture was robust enough to support a strong translation system across both benchmark settings.}\n\n\subsection{Stability Statement}\n\n\added{We do not claim a full variance analysis from the draft alone, but the supplemental five-run evidence shows a narrow spread around the main result. This supports a conservative stability statement: the headline BLEU improvements are not obviously driven by a single unstable run, although broader robustness analysis remains future work.}\n\n\subsection{Interpretability}\n\n\replaced{Qualitatively, some attention heads appear to focus on linguistically meaningful relationships. In one long English-German sentence, an upper-layer head links a subordinate-clause introducer to the postponed main verb, while another head tracks a named entity span across punctuation boundaries. We treat this as an illustrative case study rather than a complete explanation of model behavior.}{Qualitatively, some attention heads appear to focus on linguistically meaningful relationships. This suggests that the model may also offer a more interpretable view of sequence transduction behavior than purely recurrent alternatives.}\n | \section{Results}\n\n\subsection{Machine Translation}\n\nThe Transformer improves over strong prior baselines on both WMT 2014 English-to-German and English-to-French. The larger model achieves a new state of the art on English-to-German and remains highly competitive on English-to-French while requiring substantially less reported training cost than earlier recurrent or convolutional systems under the stated accounting convention.\n\n\begin{table}[h]\n\centering\n\caption{Headline comparison on WMT 2014 translation benchmarks.}\n\label{tab:headline-results}\n\begin{tabular}{lcc}\n\toprule\nModel & EN-DE BLEU & EN-FR BLEU \\\n\midrule\nGNMT + RL & 24.6 & 39.92 \\\nConvS2S & 25.16 & 40.46 \\\nTransformer (base) & 27.3 & 38.1 \\\nTransformer (big) & 28.4 & 41.0 \\\n\bottomrule\n\end{tabular}\n\end{table}\n\nThe baseline gain is not only a throughput story. In the main comparison, the attention-only encoder-decoder appears to benefit from short cross-token path lengths, which helps preserve long-range dependencies without recurrent state propagation. This gives a plausible explanation for why the model improves over baselines that remain more sequential in how they transport context.\n\n\subsection{Model Variations}\n\nWe also tested several variations of the Transformer family during development. Single-head attention underperformed the default multi-head setting, removing dropout reduced generalization quality, and scaling the model improved BLEU without eliminating the contribution of the attention design. These patterns support the claim that the gains are not explained by model size alone.\n\n\subsection{Stability Statement}\n\nWe do not claim a full variance analysis from the draft alone, but the supplemental five-run evidence shows a narrow spread around the main result. This supports a conservative stability statement: the headline BLEU improvements are not obviously driven by a single unstable run, although broader robustness analysis remains future work.\n\n\subsection{Interpretability}\n\nQualitatively, some attention heads appear to focus on linguistically meaningful relationships. In one long English-German sentence, an upper-layer head links a subordinate-clause introducer to the postponed main verb, while another head tracks a named entity span across punctuation boundaries. We treat this as an illustrative case study rather than a complete explanation of model behavior.\n | Replace sections/results.tex with the revised version for the runtime replay. |
| 7 | atomic_008 | 1 | 1 | sections/discussion.tex | replace | \section{Discussion}\n\nThe main takeaway from these experiments is that attention can serve as the primary computational primitive in a strong translation model. The resulting system trains efficiently and produces excellent benchmark performance.\n\nAt the same time, there are still practical questions around scaling, evaluation breadth, and behavior outside the current benchmark setting. These questions are important directions for future work, but they do not change the main empirical finding that attention-first sequence transduction is viable.\n | \section{Discussion}\n\nThe main takeaway from these experiments is that attention can serve as the primary computational primitive in a strong translation model. The resulting system trains efficiently and produces excellent benchmark performance.\n\n\added{The evidence is still bounded. The current results are tied to WMT 2014 machine translation and do not by themselves establish that self-attention is universally preferable for every sequence task.}\n\n\added{Failure analysis also remains important. Rare words, long subordinate-clause constructions, and some coverage-sensitive cases still account for a meaningful share of residual errors, even though aggregate BLEU improves over prior systems.}\n\n\replaced{Finally, self-attention offers short information paths but can become expensive for very long inputs. The qualitative attention examples are informative, yet they should be interpreted as illustrative analysis rather than definitive mechanistic proof.}{At the same time, there are still practical questions around scaling, evaluation breadth, and behavior outside the current benchmark setting. These questions are important directions for future work, but they do not change the main empirical finding that attention-first sequence transduction is viable.}\n | \section{Discussion}\n\nThe main takeaway from these experiments is that attention can serve as the primary computational primitive in a strong translation model. The resulting system trains efficiently and produces excellent benchmark performance.\n\nThe evidence is still bounded. The current results are tied to WMT 2014 machine translation and do not by themselves establish that self-attention is universally preferable for every sequence task.\n\nFailure analysis also remains important. Rare words, long subordinate-clause constructions, and some coverage-sensitive cases still account for a meaningful share of residual errors, even though aggregate BLEU improves over prior systems.\n\nFinally, self-attention offers short information paths but can become expensive for very long inputs. The qualitative attention examples are informative, yet they should be interpreted as illustrative analysis rather than definitive mechanistic proof.\n | Replace sections/discussion.tex with the revised version for the runtime replay. |
| 8 | atomic_008 | 1 | 1 | sections/conclusion.tex | replace | \section{Conclusion}\n\nWe introduced the Transformer, an attention-based sequence transduction model that removes recurrence and convolution from the core architecture. Across standard machine translation benchmarks, the model reaches state-of-the-art quality with strong computational efficiency.\n\nThese findings suggest that attention-based modeling may become a general foundation for many sequence-processing tasks beyond the translation setting studied here. We expect this line of work to remain broadly useful as larger datasets and broader modalities are considered.\n | \section{Conclusion}\n\nWe introduced the Transformer, an attention-based sequence transduction model that removes recurrence and convolution from the core architecture. Across standard machine translation benchmarks, the model reaches state-of-the-art quality with strong computational efficiency under the reported hardware and cost assumptions.\n\n\replaced{These findings suggest that attention-based modeling is a strong foundation for large-scale translation. Claims beyond that scope should be made cautiously: the present evidence is strongest for the benchmark tasks studied here, and broader modality or sequence-length generalization remains future work.}{These findings suggest that attention-based modeling may become a general foundation for many sequence-processing tasks beyond the translation setting studied here. We expect this line of work to remain broadly useful as larger datasets and broader modalities are considered.}\n | \section{Conclusion}\n\nWe introduced the Transformer, an attention-based sequence transduction model that removes recurrence and convolution from the core architecture. Across standard machine translation benchmarks, the model reaches state-of-the-art quality with strong computational efficiency under the reported hardware and cost assumptions.\n\nThese findings suggest that attention-based modeling is a strong foundation for large-scale translation. Claims beyond that scope should be made cautiously: the present evidence is strongest for the benchmark tasks studied here, and broader modality or sequence-length generalization remains future work.\n | Replace sections/conclusion.tex with the revised version for the runtime replay. |

