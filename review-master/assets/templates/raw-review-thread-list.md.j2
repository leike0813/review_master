# {{ msg("view.raw_threads.title") }}

{{ msg("view.raw_threads.intro") }}

## {{ msg("view.raw_threads.table") }}

| `thread_id` | `reviewer_id` | `thread_order` | `source_type` | {{ msg("view.raw_threads.normalized_summary") }} | {{ msg("view.raw_threads.linked_atomic_comments") }} |
| --- | --- | --- | --- | --- | --- |
{% for row in rows -%}
| {{ row.thread_id }} | {{ row.reviewer_id }} | {{ row.thread_order }} | {{ row.source_type }} | {{ row.normalized_summary }} | {{ row.linked_comment_ids or '' }} |
{% else -%}
|  |  |  |  |  |  |
{% endfor %}

## {{ msg("view.raw_threads.original_thread_text") }}

{% for row in rows -%}
### {{ row.thread_id }}

- {{ msg("view.raw_threads.original_text_primary_supporting") }}:

<div style="white-space: pre-wrap;">{{ row.aggregated_original_text }}</div>

{% else -%}
- {{ msg("common.none") }}
{% endfor %}
