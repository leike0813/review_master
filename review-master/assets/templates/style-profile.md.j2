# Style Profile

这是从 `review-master.db` 渲染出的 Stage 6 全局风格画像视图。数据库真源是 `style_profiles` 与 `style_profile_rules`。Stage 6 必须先完成风格画像，再进入文案版本生成与最终成文。

{% for profile in profiles -%}
## {{ profile.profile_target }}

- Summary: {{ profile.profile_summary }}
- Anti-AI Focus: {{ profile.anti_ai_focus }}

| Rule Order | Rule Type | Rule Text |
| --- | --- | --- |
{% for rule in profile.rules -%}
| {{ rule.rule_order }} | {{ rule.rule_type }} | {{ rule.rule_text }} |
{% else -%}
|  |  |  |
{% endfor %}

{% else -%}
## No Style Profiles

No style profile data available.
{% endfor %}
