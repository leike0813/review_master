# Export Patch Plan

这是从 `review-master.db` 渲染出的 Stage 6 导出补丁计划视图。数据库真源是 `export_patch_sets` 与 `export_patches`。导出脚本只负责复制导出副本，并按这里对应的显式锚点执行插入或替换；它不负责语义判断，也不负责生成最终文案。

## evidence_clean_patch_set

- Artifact kind: clean_manuscript
- Source root: /home/joshua/Workspace/Code/Skill/review_master/playbooks/examples/evidence-supplement-multi-review/inputs/manuscript
- Output root: /home/joshua/Workspace/Code/Skill/review_master/playbooks/examples/evidence-supplement-multi-review/outputs/revised-manuscript
- Status: ready

| Order | Comment | Action | Location | Target File | Operation | Anchor Text | Marked Text | Clean Text | Notes |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 1 | atomic_003 | 1 | 1 | sections/method.tex | replace | \section{Method}\nOur model uses a transformer encoder to fuse image tokens with spectral descriptors before classification.\n\n\subsection{Training Setup}\nWe train the model with a single default split and report the final accuracy on the held-out test set.\n | \section{Method}\nOur model uses a transformer encoder to fuse image tokens with spectral descriptors before classification.\n\n\subsection{\added{Ablation Setup}}\n\added{We added an ablation summary showing that removing either the image branch or the spectral branch weakens performance, indicating that the gain comes from complementary cross-modal evidence rather than a single dominant stream.}\n\n\subsection{Training Setup}\n\replaced{We use a fixed train/validation/test split of 640/160/200 plots and repeat all stochastic experiments with seeds 11, 17, 23, 29, and 37.}{We train the model with a single default split and report the final accuracy on the held-out test set.}\n | \section{Method}\nOur model uses a transformer encoder to fuse image tokens with spectral descriptors before classification.\n\n\subsection{Ablation Setup}\nWe added an ablation summary showing that removing either the image branch or the spectral branch weakens performance, indicating that the gain comes from complementary cross-modal evidence rather than a single dominant stream.\n\n\subsection{Training Setup}\nWe use a fixed train/validation/test split of 640/160/200 plots and repeat all stochastic experiments with seeds 11, 17, 23, 29, and 37.\n | Replace full section file in clean export. |
| 2 | atomic_001 | 1 | 1 | sections/results.tex | replace | \section{Results}\n\subsection{Main Results}\nThe multimodal transformer improves top-1 accuracy over the CNN and spectral-only baselines on the field-test benchmark.\n | \section{Results}\n\subsection{Main Results}\n\replaced{The multimodal encoder improves separation because contextual fusion reduces confusion between visually similar stress patterns while preserving spectral contrast cues.}{The multimodal transformer improves top-1 accuracy over the CNN and spectral-only baselines on the field-test benchmark.}\n\n\subsection{\added{Ablation Discussion}}\n\added{Removing either the image branch or the spectral branch weakens performance, indicating that the gain comes from complementary cross-modal evidence rather than a single dominant stream.}\n\n\subsection{\added{Seed Stability Experiment}}\n\added{Following the reviewer request, we added a five-seed stability experiment. Across five random seeds, the mean macro-F1 remains stable and the spread stays narrow enough to support the robustness claim.}\n\n\begin{figure}[h]\n\centering\n\includegraphics[width=0.72\\linewidth]{figures/seed-stability-figure.svg}\n\caption{\added{Top-1 accuracy across five random seeds for the multimodal transformer.}}\n\end{figure}\n | \section{Results}\n\subsection{Main Results}\nThe multimodal encoder improves separation because contextual fusion reduces confusion between visually similar stress patterns while preserving spectral contrast cues.\n\n\subsection{Ablation Discussion}\nRemoving either the image branch or the spectral branch weakens performance, indicating that the gain comes from complementary cross-modal evidence rather than a single dominant stream.\n\n\subsection{Seed Stability Experiment}\nFollowing the reviewer request, we added a five-seed stability experiment. Across five random seeds, the mean macro-F1 remains stable and the spread stays narrow enough to support the robustness claim.\n\n\begin{figure}[h]\n\centering\n\includegraphics[width=0.72\\linewidth]{figures/seed-stability-figure.svg}\n\caption{Top-1 accuracy across five random seeds for the multimodal transformer.}\n\end{figure}\n | Replace full section file in clean export. |
| 3 | atomic_005 | 1 | 1 | sections/discussion.tex | replace | \section{Discussion}\nThe current discussion only briefly mentions limitations and deployment risks.\n | \section{Discussion}\n\replaced{Our evaluation is limited to the current sensing setup and still requires deployment-time calibration under distribution shift.}{The current discussion only briefly mentions limitations and deployment risks.}\n | \section{Discussion}\nOur evaluation is limited to the current sensing setup and still requires deployment-time calibration under distribution shift.\n | Replace full section file in clean export. |

## evidence_marked_patch_set

- Artifact kind: marked_manuscript
- Source root: /home/joshua/Workspace/Code/Skill/review_master/playbooks/examples/evidence-supplement-multi-review/inputs/manuscript
- Output root: /home/joshua/Workspace/Code/Skill/review_master/playbooks/examples/evidence-supplement-multi-review/outputs/marked-manuscript
- Status: ready

| Order | Comment | Action | Location | Target File | Operation | Anchor Text | Marked Text | Clean Text | Notes |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 1 | atomic_003 | 1 | 1 | sections/method.tex | replace | \section{Method}\nOur model uses a transformer encoder to fuse image tokens with spectral descriptors before classification.\n\n\subsection{Training Setup}\nWe train the model with a single default split and report the final accuracy on the held-out test set.\n | \section{Method}\nOur model uses a transformer encoder to fuse image tokens with spectral descriptors before classification.\n\n\subsection{\added{Ablation Setup}}\n\added{We added an ablation summary showing that removing either the image branch or the spectral branch weakens performance, indicating that the gain comes from complementary cross-modal evidence rather than a single dominant stream.}\n\n\subsection{Training Setup}\n\replaced{We use a fixed train/validation/test split of 640/160/200 plots and repeat all stochastic experiments with seeds 11, 17, 23, 29, and 37.}{We train the model with a single default split and report the final accuracy on the held-out test set.}\n | \section{Method}\nOur model uses a transformer encoder to fuse image tokens with spectral descriptors before classification.\n\n\subsection{Ablation Setup}\nWe added an ablation summary showing that removing either the image branch or the spectral branch weakens performance, indicating that the gain comes from complementary cross-modal evidence rather than a single dominant stream.\n\n\subsection{Training Setup}\nWe use a fixed train/validation/test split of 640/160/200 plots and repeat all stochastic experiments with seeds 11, 17, 23, 29, and 37.\n | Replace full section file in marked export. |
| 2 | atomic_001 | 1 | 1 | sections/results.tex | replace | \section{Results}\n\subsection{Main Results}\nThe multimodal transformer improves top-1 accuracy over the CNN and spectral-only baselines on the field-test benchmark.\n | \section{Results}\n\subsection{Main Results}\n\replaced{The multimodal encoder improves separation because contextual fusion reduces confusion between visually similar stress patterns while preserving spectral contrast cues.}{The multimodal transformer improves top-1 accuracy over the CNN and spectral-only baselines on the field-test benchmark.}\n\n\subsection{\added{Ablation Discussion}}\n\added{Removing either the image branch or the spectral branch weakens performance, indicating that the gain comes from complementary cross-modal evidence rather than a single dominant stream.}\n\n\subsection{\added{Seed Stability Experiment}}\n\added{Following the reviewer request, we added a five-seed stability experiment. Across five random seeds, the mean macro-F1 remains stable and the spread stays narrow enough to support the robustness claim.}\n\n\begin{figure}[h]\n\centering\n\includegraphics[width=0.72\\linewidth]{figures/seed-stability-figure.svg}\n\caption{\added{Top-1 accuracy across five random seeds for the multimodal transformer.}}\n\end{figure}\n | \section{Results}\n\subsection{Main Results}\nThe multimodal encoder improves separation because contextual fusion reduces confusion between visually similar stress patterns while preserving spectral contrast cues.\n\n\subsection{Ablation Discussion}\nRemoving either the image branch or the spectral branch weakens performance, indicating that the gain comes from complementary cross-modal evidence rather than a single dominant stream.\n\n\subsection{Seed Stability Experiment}\nFollowing the reviewer request, we added a five-seed stability experiment. Across five random seeds, the mean macro-F1 remains stable and the spread stays narrow enough to support the robustness claim.\n\n\begin{figure}[h]\n\centering\n\includegraphics[width=0.72\\linewidth]{figures/seed-stability-figure.svg}\n\caption{Top-1 accuracy across five random seeds for the multimodal transformer.}\n\end{figure}\n | Replace full section file in marked export. |
| 3 | atomic_005 | 1 | 1 | sections/discussion.tex | replace | \section{Discussion}\nThe current discussion only briefly mentions limitations and deployment risks.\n | \section{Discussion}\n\replaced{Our evaluation is limited to the current sensing setup and still requires deployment-time calibration under distribution shift.}{The current discussion only briefly mentions limitations and deployment risks.}\n | \section{Discussion}\nOur evaluation is limited to the current sensing setup and still requires deployment-time calibration under distribution shift.\n | Replace full section file in marked export. |

